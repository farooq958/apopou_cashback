import 'package:cashback/model/parent_categories.dart';

class ParentCategoriesController{
  static ParentCategories data=ParentCategories(data: [], meta: Meta(
      pagination: Pagination(
          currentPage: 0,
          links: Links(),
          perPage: 0,
          count: 0,
          total: 9,
          totalPages: 34)));
}